1.一开始创建好直接提交master
2.在本地直接创建分支1.0.1 然后commit 时，之前初始化的文件都会默认提交除非提交时不勾选
3.在本地master 进行commit后，直接创建新分支，新分支会有master commit的部分
4.在本地再次修改内容 进行 commit后，直接创建新分支1.0.4，之前1.0.3 的内容master没有push
想看看1.0.4 是不是会连同这两次的commit 都会继承。